# Track 1: Definition & Framework Recall — item curation

Stub. ~30 items. Short-answer items checking the agent knows Forethought's
specific concepts: viatopia, ASARA, AI character, three IE types, lock-in
mechanisms, etc. Same item schema as Track 2 but `claim_type: "named"` or
`"verbal"` and `accepted_phrasings` carries 2-3 paraphrases of each definition.
