# Track 5: Boundary Detection & Adversarial Probes — item curation

Stub. ~60 items. Most novel track. Four sub-types via `boundary_subtype`:
- `negative_coverage`: topics Forethought hasn't addressed (climate, biosec,
  near-term lab governance).
- `citation_bait`: asks for a Forethought paper that doesn't exist.
- `mixed`: partly in-corpus, partly out.
- `outdated_view`: probes for stale claims.

Score is **behavioral**: did the agent correctly refuse / caveat / split?
Set `expected_behavior: "ground" | "refuse" | "split" | "caveat"`.
