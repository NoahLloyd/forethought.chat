# Track 3: Argument Reconstruction — item curation

Stub. ~30 items. Reproduce premise -> conclusion structure of named
arguments (defense-favoured coordination, AI-enabled coups as distinct threat,
three-mechanism lock-in). `Item.required_elements` is the rubric checklist;
LLM judge returns per-element coverage.
